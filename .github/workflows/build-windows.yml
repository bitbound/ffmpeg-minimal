name: Build Windows

on:
  workflow_dispatch:
    inputs:
      capture_device:
        description: "Screen capture device to use"
        required: true
        default: "gdigrab"
        type: choice
        options:
          - ddagrab
          - gdigrab
  workflow_call:
    inputs:
      capture_device:
        description: "Screen capture device to use"
        required: true
        default: "gdigrab"
        type: string

jobs:
  build-windows:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - msystem: MINGW64
            arch: x86_64
            artifact_name: ffmpeg-win64
          - msystem: MINGW32
            arch: i686
            artifact_name: ffmpeg-win32

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: ${{ matrix.msystem }}
          update: true
          install: >-
            base-devel
            mingw-w64-${{ matrix.arch }}-gcc
            mingw-w64-${{ matrix.arch }}-yasm
            mingw-w64-${{ matrix.arch }}-nasm
            mingw-w64-${{ matrix.arch }}-pkg-config
            git

      - name: Build libvpx
        shell: msys2 {0}
        run: |
          git clone --depth 1 --branch v1.14.1 https://chromium.googlesource.com/webm/libvpx
          cd libvpx
          ./configure \
            --prefix=/mingw${{ matrix.arch == 'x86_64' && '64' || '32' }} \
            --target=${{ matrix.arch == 'x86_64' && 'x86_64-win64-gcc' || 'x86-win32-gcc' }} \
            --disable-examples \
            --disable-tools \
            --disable-docs \
            --disable-unit-tests \
            --enable-vp9 \
            --enable-vp9-highbitdepth
          make -j$(nproc)
          make install

      - name: Build FFmpeg
        shell: msys2 {0}
        run: |
          git clone --depth 1 --branch release/7.1 https://git.ffmpeg.org/ffmpeg.git
          cd ffmpeg
          ./configure \
            --prefix=$PWD/../output \
            --pkg-config-flags="--static" \
            --disable-everything \
            --enable-libvpx \
            --enable-indev=dshow \
            --enable-indev=${{ inputs.capture_device }} \
            --enable-indev=lavfi \
            --enable-decoder=rawvideo \
            --enable-decoder=vp9 \
            --enable-demuxer=rawvideo \
            --enable-encoder=libvpx_vp9 \
            --enable-protocol=pipe \
            --enable-muxer=webm \
            --enable-muxer=matroska \
            --enable-filter=scale \
            --enable-filter=format \
            --disable-doc \
            --disable-debug \
            --extra-ldflags="-static"
          make -j$(nproc)
          make install
          strip ../output/bin/ffmpeg.exe

      - name: Prepare Windows artifact
        shell: pwsh
        run: |
          New-Item -Path artifact -ItemType Directory -Force | Out-Null
          Copy-Item -Path output/bin/ffmpeg.exe -Destination artifact/${{ matrix.artifact_name }}-${{ inputs.capture_device }}.exe
          Copy-Item -Path THIRD_PARTY_NOTICES.txt -Destination artifact

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact_name }}-${{ inputs.capture_device }}
          path: artifact/*
          if-no-files-found: error
          retention-days: 1
